========================================================================

v1.3.2

fix: Enable capability-based File System Access downloads by default while preserving automatic native download fallback for unsupported browsers.


========================================================================

v1.3.1

fix: Route the single Download action by effective browser capability and the site gate, preserving system file selection where File System Access is available and using the native download manager otherwise.
perf: Replace range probes, parallel client segments, and large write queues with one authenticated, backpressured download stream.
fix: Explain native download-location behavior until the user dismisses the guidance through their synced Vault preferences.


========================================================================

v1.3.0

fix: Set frontend cache headers so deployments fetch the current app shell while content-addressed assets remain safely cacheable.
fix: Make upload resume explicit by discarding zero-byte stored sessions, resuming only committed parts for the same selected file, and showing resume state in the UI.
build: Centralize release version metadata on the VERSION file and publish Docker images from semantic version tags plus latest.


========================================================================

v1.2.0

fix: Harden resumable upload parts with browser-wide concurrency limits, per-part timeouts, and retries for transport-shaped upload failures.
fix: Prevent failed or stalled upload part requests from leaving concurrent uploads permanently blocked.
docs: Update Docker release defaults for v1.2.0.


========================================================================

v1.1.0

feat: Rewrite the application backend in Rust with an Axum/SQLx service that preserves the existing API and single-container deployment contract.
feat: Port authentication, OIDC, security headers, gzip, admin/debug tooling, bootstrap metadata, preferences, site settings, and live state events to the Rust runtime.
feat: Port folder browsing, document metadata, version history, locks, checkouts, archive/restore, favorites, sharing, retention, uploads, downloads, and export workflows to Rust.
feat: Add Rust-native resumable transfer sessions, transfer cleanup, folder export jobs, ZIP64 archive support, and local/S3/R2 object storage handling.
docs: Document the Rust rewrite and update Docker release defaults for v1.1.0.


========================================================================

v1.0.1

fix: Harden SQLite event streaming under concurrent use by moving state-event database reads off the ASGI event loop.
fix: Wake idle SSE clients from committed state-change notifications instead of per-client polling.
fix: Align SQLite busy timeout and SQLAlchemy pool configuration for the single-container deployment profile.
fix: Keep file extensions out of the default rename selection unless the user explicitly edits them.
fix: Show Remove from Favorites when a selected item is opened from the Favorites context menu.
fix: Prevent archived files from being renamed in the UI, inline rename flow, and rename API.


========================================================================

v1.0.0

Initial release


========================================================================

feat:     A new feature.
fix:      A bug fix.
docs:     Documentation changes.
style:    Code style changes (formatting, missing semicolons, etc.).
refactor: Code refactoring (neither fixes a bug nor adds a feature).
test:     Adding or updating tests.
chore:    Routine tasks like updating dependencies or build tools.
build:    Changes affecting the build system or external dependencies.
ci:       Changes to CI configuration files or scripts.
perf:     Performance improvements.
revert:   Reverting a previous commit.
