Program icons live here when they are project-hosted raster assets instead of FontAwesome
definitions. Add the image file, then register it with `imageIconEntry` in
`app/static/js/lib/IconLibrary.js`.

Use lowercase kebab-case filenames and stable registry ids prefixed with `app-`, because
folder appearance choices are stored by id.
